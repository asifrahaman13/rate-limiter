init system
top level code
require modules (import modules)
call backs are registered. Does not mean it will run. It only means it will register.

Start event || Thread pools -> CPU intensive tasks are executed by different threads than the main threads.

By default it is 4 threads and can go upto 128 threads. 
